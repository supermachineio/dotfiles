# AWS aliases
alias awslogin="eval $(aws ecr get-login --no-include-email)"

# Git aliases
alias gitlog='git log -n 10'

# Handy defaults
alias grep='grep --color=auto '
alias untar="tar -xvf"
alias xclip="xclip -selection c"
